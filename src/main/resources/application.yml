server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/victormartingil/cloud-config-server/tree/master/src/main/resources
          clone-on-start: true